let userDefinedNumber = 8;
while (userDefinedNumber > 0 ) {
    if ( userDefinedNumber === 5 ) {
        console.log ("Almost there!");
    } else if (userDefinedNumber === 1 ) {
        console.log ("Liftoff!")
        }
    else {
        console.log(userDefinedNumber);
    }
    userDefinedNumber--;
}